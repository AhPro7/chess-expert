# Data (large) — download / regenerate locally
data/*.pgn
data/*.zip
data/*.npz
data/samples/

# TensorBoard logs (uploaded to HF Hub instead of git)
runs/

# Generated game media (large; regenerate with scripts/showcase.py / win_reel.py)
demo/*.mp4
demo/*.gif
demo/*.pgn
demo/wins/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store

# Keep the shipped demo checkpoint (small) tracked; ignore ad-hoc ones.
models/*.pt
!models/chess_expert.pt
